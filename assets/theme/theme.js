import { createTheme } from '@material-ui/core/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ffffff',
      main: '#cfd8dc',
      dark: '#9ea7aa'
    },
    secondary: {
      light: '#cf81ff',
      main: '#9B51E0',
      dark: '#6820ad'
    },
  },
});
